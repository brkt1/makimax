{
  "name": "your-project-name",
  "version": "1.0.0",
  "homepage": "https://brkt1.github.io/makimax",
  "scripts": {
    "start": "your-start-command",
    "build": "your-build-command",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
  },
  "devDependencies": {
    "gh-pages": "^3.2.3" 
  }
}
